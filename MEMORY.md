# Long-term Memory

## 2026-07-19
- Implementing account deletion workflow with admin verification.
- Updating UI to display `FirstName` instead of `uid` across various screens.
- Identified existing placeholder code in `AdminPageWidget` for delete requests and correcting it.
